<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Todoapp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="tint">@color/light_background</item>
        <item name="colorPrimary">@color/light_background</item>
        <item name="android:backgroundTint">@color/dark_background</item>
        <item name="android:navigationBarColor">@color/dark_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="AppTabLayout" parent="Widget.Design.TabLayout">
        <item name="backgroundTint">#fff</item>
        <item name="tabIndicatorHeight">0dp</item>
        <item name="tabSelectedTextColor">@color/light_background</item>
    </style>
</resources>